html(lang="en")
    head
        meta(charset="UTF-8")
        meta(http-equiv="X-UA-Compatible", content="IE=edge")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        style 
            include ./email.css 
    body 
        h2 Confirma tu cuenta UpTask
        p Ya estas casi inscrito, solo presiona este boton
        a(href=`${confirmarUrl}`) Confirmar Cuenta
        p Si no puedes aacceder a este enlace visita #{confirmarUrl}
        p si no solicitaste este email puedes ignorarlo